# Scoped to this directory so the project's root .gitignore stays untouched.
__pycache__/
*.pyc

# Per-run bookkeeping for ingestion_summary.md (failures + companion-file
# lists). Regenerated by collect.py; the published summary is the artifact.
_ingest/.state.json
